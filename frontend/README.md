# FoodHub Web Frontend

React + TypeScript frontend for FoodHub.

## Run

```bash
cd frontend
npm install
npm run dev
```

## Notes

- Vite proxies `/api` to `http://localhost:3000`.
- Set `VITE_API_BASE_URL` if the backend runs elsewhere.